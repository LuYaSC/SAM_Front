import { ControlGiftResult } from './control-gift-result';

describe('ControlGiftResult', () => {
  it('should create an instance', () => {
    expect(new ControlGiftResult()).toBeTruthy();
  });
});
