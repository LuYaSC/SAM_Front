import { OfficesPlacesResult } from './offices-places-result';

describe('OfficesPlacesResult', () => {
  it('should create an instance', () => {
    expect(new OfficesPlacesResult()).toBeTruthy();
  });
});
